.DS_Store
files/
node_modules/
test-results/
.artifacts/
playwright-report/
output/
audio/ava/tmp/

# audio generation outputs (keep tracked only when intentionally committing packs)
audio/ava/**/tmp/
audio/**/.cache/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local Codex phase notes
CODEX_PHASE_*.txt

# Curriculum zip files (large binaries — not tracked in git)
*.zip
.claire/
.claude/

# AI config (contains Azure API key — user-created, not committed)
js/ai-config.js

# Environment variables (contains secrets, never commit)
.env
.env.local
.env.*.local
.env.production
.env.production.local
.env.development.local

# Database
server/database/*.log
server/database/backups/
server/database/migrations/
logs/
*.sql.gz

# Large/temporary directories
Music for CornerstoneMTSS/
artifacts/
test-results/
coverage/

# Vite
.vite/
js/dist/
